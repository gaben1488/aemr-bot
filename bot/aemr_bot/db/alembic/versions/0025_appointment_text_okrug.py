"""Текст приёма граждан: «района» → «округа» на работающей установке.

Дефолт в `services/settings_store.DEFAULTS` исправлен решением владельца
после принятия устава Елизовского муниципального округа, но до жителя он
не доедет: `appointment_text` входит в синхронизируемые ключи, поэтому на
работающем боте значение уже лежит строкой в `settings`, а дефолт
применяется только при её отсутствии. Пути «репозиторий → база» в проекте
нет — синхронизация работает в обратную сторону (база → PR). Без этой
миграции житель на экране «Приём граждан» продолжал бы читать «района»,
пока оператор не перепечатает текст руками.

Обновляем ТОЛЬКО строку, дословно совпадающую со старым дефолтом. Если
Администрация уже правила текст под себя (сменился график, номер
телефона, исполняющий полномочия) — значение чужое, и миграция обязана
пройти мимо: затирать ручную правку хуже, чем оставить старое название.

Revision ID: 0025
Revises: 0024
Create Date: 2026-08-07
"""
from collections.abc import Sequence

from alembic import op
from sqlalchemy import text

revision: str = "0025"
down_revision: str | None = "0024"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None

_OLD = (
    "Приём граждан временно исполняющим полномочия Главы Елизовского "
    "муниципального района А.С. Гончаровым осуществляется два раза в месяц "
    "(1 и 3 среда каждого месяца) по предварительной записи. "
    "Запись на приём ведётся по номеру телефона 8 (415-31) 7-25-29."
)
_NEW = _OLD.replace("муниципального района", "муниципального округа")


def _swap(before: str, after: str) -> None:
    # `settings.value` — колонка JSONB, текст лежит в ней JSON-строкой.
    # Голая подстановка даёт «invalid input syntax for type json», а
    # сравнение с сырым текстом не нашло бы строку даже при удачной
    # записи. Поэтому `to_jsonb(...::text)` на запись и `#>> '{}'` —
    # извлечение скалярной строки — на сравнение.
    op.get_bind().execute(
        text(
            """
            UPDATE settings
               SET value = to_jsonb(CAST(:after AS text))
             WHERE key = 'appointment_text'
               AND value #>> '{}' = :before
            """
        ),
        {"before": before, "after": after},
    )


def upgrade() -> None:
    _swap(_OLD, _NEW)


def downgrade() -> None:
    # Симметрично: возвращаем «района» только если текст не трогали после
    # обновления. Правку оператора откат тоже обязан пропустить.
    _swap(_NEW, _OLD)
